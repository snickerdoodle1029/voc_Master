# 🚀 快速启动指南

## 本地运行

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 运行应用
```bash
streamlit run app.py
```

或者使用启动脚本：
```bash
bash run.sh
```

### 3. 访问应用
浏览器会自动打开，或手动访问：`http://localhost:8501`

## 部署到云端（推荐）

### Streamlit Cloud 部署（免费）

1. **准备代码**
   - 将所有代码推送到 GitHub 仓库
   - 确保包含 `requirements.txt` 和 `app.py`

2. **部署步骤**
   - 访问 https://streamlit.io/cloud
   - 使用 GitHub 账号登录
   - 点击 "New app"
   - 选择你的仓库
   - 设置 Main file path 为: `app.py`
   - 点击 "Deploy"

3. **分享链接**
   - 部署完成后，Streamlit Cloud 会提供一个公开链接
   - 例如：`https://your-app-name.streamlit.app`
   - 将这个链接分享给用户即可

### 其他部署选项

- **Heroku**: 需要添加 `Procfile` 和 `runtime.txt`
- **Railway**: 支持直接从 GitHub 部署
- **Render**: 支持 Python Web 服务
- **Vercel**: 需要配置 Python 运行时

## 使用说明

1. 在网页上输入评论列表（每行一条）
2. 或选择"示例数据"查看示例
3. 点击"开始分析"按钮
4. 查看分析结果
5. 可选择下载 Markdown 格式的报告

## 注意事项

- 确保 Python 版本 >= 3.7
- 首次运行会自动安装 Streamlit
- 如需自定义端口：`streamlit run app.py --server.port 8502`
